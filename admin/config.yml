backend:
  name: github
  repo: PerpetualStyle/www.perpetualstyle.co.za
  branch: main # or your default branch
  base_url: https://dev-4a45a4rlewxn1ja0.us.auth0.com # Your site URL
  auth_endpoint: /authorize?response_type=code&client_id=Ov23li3lL1IFRemCcUIx&redirect_uri=https://www.perpetualstyle.co.za/admin/&scope=repo%20user
media_folder: "static/images" # Folder for uploaded images
public_folder: "/images" # Public path for images
collections:
  - name: "posts"
    label: "Posts"
    folder: "_posts" # Folder where Markdown files are stored
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { name: "title", label: "Title", widget: "string" }
      - { name: "date", label: "Date", widget: "datetime" }
      - { name: "body", label: "Body", widget: "markdown" }
